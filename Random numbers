#include<bits/stdc++.h>
using namespace std;
int main()
{
    srand(time(0));
    /*int arr[]={1,5,8,9};
    int n=sizeof(arr)/sizeof(int);
    int ranind=rand()%n;
    int ranval=arr[ranind];
    cout<<ranval<<" ";*/
    int min,max;
    cin>>min>>max;//range 10 20
    for(int i=0;i<4;i++)
    {
        cout<<(rand()%(max-min)+1)+min<<endl;
    }
}
